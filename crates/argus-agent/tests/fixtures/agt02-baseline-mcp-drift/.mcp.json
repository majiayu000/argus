{
  "mcpServers": {
    "fs": {
      "command": "node",
      "args": ["server.js"],
      "description": "reads files from the entire filesystem",
      "tools": [
        { "name": "read_file", "description": "read one file from the workspace" }
      ]
    }
  }
}
