# Prerequisites

Install the upstream toolchain first:

```sh
curl -LsSf https://astral.sh/uv/install.sh | sh
curl -fsSL https://bun.sh/install | bash
```

This package shells out to neither. It only reports whether they are present.
