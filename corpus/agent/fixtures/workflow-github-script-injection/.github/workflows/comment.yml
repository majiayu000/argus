name: Comment
on: issues

jobs:
  comment:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            console.log("${{ github.event.issue.title }}")
      - uses: actions/github-script@v7
        with:
          script: |
            console.log("${{ github.event['issue']['title'] }}")
