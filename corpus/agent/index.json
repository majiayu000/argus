{
  "schemaVersion": 1,
  "surface": "agent-skill",
  "evaluation": {
    "schemaVersion": 1,
    "datasetType": "synthetic-fixtures",
    "labelSource": "original six maintainer-reviewed expectedDecision values from GH-58 PR #60; system-override regressions from GH-196 PR #197 and GH-202 PR #203",
    "positiveDecision": "block",
    "frozen": true
  },
  "cases": [
    {
      "id": "skill-config-backdoor",
      "kind": "fixture",
      "path": "fixtures/skill-config-backdoor",
      "packageName": "markdown-beautifier",
      "expectedDecision": "block",
      "rules": ["capability-misfit", "agent-config-write", "hook-persistence"],
      "notes": "SKILL.md claims markdown formatting; setup.sh writes ~/.claude/settings.local.json and registers an auto-approve hook. Declared intent has no reason to touch agent config."
    },
    {
      "id": "skill-cred-exfil",
      "kind": "fixture",
      "path": "fixtures/skill-cred-exfil",
      "packageName": "repo-stats",
      "expectedDecision": "block",
      "rules": ["capability-misfit", "credential-access", "network-exfiltration"],
      "notes": "SKILL.md claims repo stats; collect.sh reads ~/.aws/credentials + .env + ANTHROPIC_API_KEY/GITHUB_TOKEN and POSTs them to a remote host. sensitive_read + net_egress combo to non-declared host."
    },
    {
      "id": "skill-obfuscated-dropper",
      "kind": "fixture",
      "path": "fixtures/skill-obfuscated-dropper",
      "packageName": "emoji-picker",
      "expectedDecision": "block",
      "rules": ["obfuscation", "shell-pipe-execution", "remote-download"],
      "notes": "postinstall.sh does curl | base64 -d | bash plus an inline base64-decoded pipe-to-sh. Obfuscated remote stage-2 execution."
    },
    {
      "id": "skill-injection-md",
      "kind": "fixture",
      "path": "fixtures/skill-injection-md",
      "packageName": "helpful-assistant",
      "expectedDecision": "block",
      "rules": ["injection-override", "concealment"],
      "notes": "SKILL.md hides an HTML comment claiming absolute authority, telling the agent to ignore prior instructions, push to an attacker remote, and not mention it. Pure text/LLM-instruction attack (no scripts) — the dominant attack surface in the census."
    },
    {
      "id": "skill-formatted-system-override",
      "kind": "fixture",
      "path": "fixtures/skill-formatted-system-override",
      "packageName": "formatted-system-override",
      "expectedDecision": "block",
      "rules": ["injection-override"],
      "notes": "SKILL.md uses Markdown emphasis, bounded inline HTML, and a safety qualifier between `system` and the authority target noun. Pure text/LLM-instruction attack (no scripts)."
    },
    {
      "id": "skill-benign-installer",
      "kind": "fixture",
      "path": "fixtures/skill-benign-installer",
      "packageName": "python-project-init",
      "expectedDecision": "allow",
      "rules": [],
      "notes": "NEGATIVE / FP-trap. SKILL.md documents `curl https://astral.sh/uv/install.sh | sh` (official vendor installer) as a prerequisite. No bundled scripts. The current lexical curl_pipe_sh rule flags this — it must NOT block. Census showed 1571 such benign hits."
    },
    {
      "id": "skill-wbr-system-override",
      "kind": "fixture",
      "path": "fixtures/skill-wbr-system-override",
      "packageName": "wbr-system-override",
      "expectedDecision": "block",
      "rules": ["injection-override"],
      "notes": "SKILL.md isolates a no-space `<wbr>` break opportunity between `system` and the authority target. Pure text/LLM-instruction attack (no scripts)."
    },
    {
      "id": "skill-benign-net-tool",
      "kind": "fixture",
      "path": "fixtures/skill-benign-net-tool",
      "packageName": "weather-lookup",
      "expectedDecision": "allow-with-approval",
      "rules": ["capability-manifest"],
      "notes": "NEGATIVE / FP-trap. fetch.sh reads WEATHER_API_KEY + calls a weather API — capabilities present but consistent with declared intent and targeting the tool's own host. Should surface a capability manifest and require approval, NOT block. Guards against 'reads key + net egress = malicious'."
    },
    {
      "id": "skill-benign-system-override",
      "kind": "fixture",
      "path": "fixtures/skill-benign-system-override",
      "packageName": "cross-platform-theme-guide",
      "expectedDecision": "allow",
      "rules": [],
      "notes": "NEGATIVE / FP-trap. SKILL.md uses generic 'override system' phrases for UI themes, gestures, game records, and tenant branding without attempting to supersede agent instructions or policy."
    },
    {
      "id": "workflow-dangerous",
      "kind": "fixture",
      "path": "fixtures/workflow-dangerous",
      "packageName": "workflow-dangerous",
      "expectedDecision": "block",
      "rules": ["workflow-context-injection", "workflow-mutable-action", "workflow-untrusted-checkout"],
      "notes": "A privileged pull_request_target workflow checks out attacker-controlled pull-request code and directly interpolates an attacker-controlled title into a shell script."
    },
    {
      "id": "workflow-mutable-action",
      "kind": "fixture",
      "path": "fixtures/workflow-mutable-action",
      "packageName": "workflow-mutable-action",
      "expectedDecision": "allow-with-approval",
      "rules": ["workflow-mutable-action"],
      "notes": "A normal CI workflow references actions/checkout by mutable major tag, requiring explicit approval without claiming compromise."
    },
    {
      "id": "workflow-pinned-benign",
      "kind": "fixture",
      "path": "fixtures/workflow-pinned-benign",
      "packageName": "workflow-pinned-benign",
      "expectedDecision": "allow",
      "rules": [],
      "notes": "NEGATIVE / FP-trap. The remote action is pinned to a full commit SHA and untrusted PR data is passed through an environment variable instead of interpolated into run."
    },
    {
      "id": "composite-action-dangerous",
      "kind": "fixture",
      "path": "fixtures/composite-action-dangerous",
      "packageName": "composite-action-dangerous",
      "expectedDecision": "block",
      "rules": ["workflow-context-injection", "workflow-mutable-action"],
      "notes": "A local composite Action uses a mutable remote dependency and directly interpolates attacker-controlled pull-request data into an inline script."
    },
    {
      "id": "composite-action-pinned-benign",
      "kind": "fixture",
      "path": "fixtures/composite-action-pinned-benign",
      "packageName": "composite-action-pinned-benign",
      "expectedDecision": "allow",
      "rules": [],
      "notes": "NEGATIVE / FP-trap. The composite Action pins its remote dependency to a full commit SHA and passes pull-request data through an environment variable."
    }
  ]
}
