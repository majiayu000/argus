name: Argus supply-chain scan
description: Download a verified Argus release and scan a package, lockfile, or agent surface.
inputs:
  scanType:
    description: One of package, lockfile, or agent.
    required: true
  path:
    description: Workspace-relative package directory, lockfile, or agent surface.
    required: true
  githubToken:
    description: GitHub token used only for authenticated release download and attestation verification.
    required: true
  format:
    description: One of text, json, or sarif.
    required: false
    default: text
  argusVersion:
    description: Exact tested Argus version (X.Y.Z).
    required: false
  failOn:
    description: Fail on block, or on block and approval.
    required: false
    default: block
  base:
    description: Optional workspace-relative base lockfile for dependency delta admission.
    required: false
  baseLockfileFormat:
    description: Explicit format for an ambiguously named base lockfile.
    required: false
  maliciousDb:
    description: Optional workspace-relative pinned malicious-package snapshot.
    required: false
  approvalLedger:
    description: Optional workspace-relative digest- and capability-bound approval ledger.
    required: false
outputs:
  decision:
    description: Validated Argus decision; blank on operational failure.
  exitCode:
    description: Argus process exit code; blank before the scan starts.
  reportPath:
    description: Validated report path; blank on operational failure.
  sarifFile:
    description: SARIF path when format is sarif; otherwise blank.
  argusVersion:
    description: Validated requested binary version.
runs:
  using: node24
  main: action/dist/index.js
branding:
  icon: shield
  color: purple
